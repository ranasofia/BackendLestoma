import { Router } from 'express'
const router = Router()
import * as dataCtrl from '../controllers/data.controllers'

//router.get('/obtener', dataCtrl.obtenerDatos)
//srouter.get('/datos', dataCtrl.getAllData)



// app.get('/datos', controller.obtenerDatos);


export default router;